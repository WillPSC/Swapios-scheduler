"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasRequire = void 0;
const config_1 = require("../config");
const hasRequire = (j, source) => source
    .find(j.CallExpression, {
    callee: { type: "Identifier", name: "require" },
})
    .filter((callExpression) => {
    const { value: sourceValue } = callExpression.value.arguments[0];
    return typeof sourceValue === "string" && sourceValue.startsWith(config_1.PACKAGE_NAME);
})
    .size() > 0;
exports.hasRequire = hasRequire;
